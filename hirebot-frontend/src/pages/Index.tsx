import { HireBotHeader } from "@/components/HireBotHeader";
import { HireBotApp } from "@/components/HireBotApp";

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-subtle">
      <HireBotHeader />
      <HireBotApp />
    </div>
  );
};

export default Index;
